import uvicorn
from fastapi import FastAPI

app = FastAPI()

# ... your existing placement recommender AI code here ...

if __name__ == "__main__":
    # This instructs python to run the server on Render's required port
    uvicorn.run(app, host="0.0.0.0", port=10000)
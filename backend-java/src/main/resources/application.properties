# 1. New Cloud MySQL Connection Details (Hardcoded Credentials)
spring.datasource.url=jdbc:mysql://mysql-placement-db-ayaappaswami-196a.h.aivencloud.com:13826/defaultdb?useSSL=true&requireSSL=true&verifyServerCertificate=false&allowPublicKeyRetrieval=true
spring.datasource.username=avnadmin
spring.datasource.password=AVNS_ZcvrvqBHih6wtWli7qk
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# 2. Updated Hibernate/JPA Settings for MySQL
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# 3. Server Port
server.port=8080

# 4. FIX: File Upload Limits (Prevents errors with large PDFs/DOCX)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# 5. FIX: Disable Default Security (Bypasses the "Connection Error" block)
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration